apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: siegfrid-com-tls
  namespace: gateway
spec:
  secretName: siegfrid-com-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.siegfrid.com"
