apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-tls
  namespace: default
spec:
  secretName: wildcard-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  commonName: "siegfrid.com"
  dnsNames:
    - "siegfrid.com"
    - "*.siegfrid.com"
